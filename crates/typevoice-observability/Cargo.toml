[package]
name = "typevoice-observability"
version = "0.1.2"
edition = "2021"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
typevoice-core = { path = "../typevoice-core" }

[dev-dependencies]
tempfile = "3"
